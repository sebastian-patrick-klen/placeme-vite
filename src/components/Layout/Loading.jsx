const Loading = ({}) => {
  return (
    <div className='w-full calc-height flex items-center justify-center'>
      <h3 className=' text-xl animate-pulse'>Načítání...</h3>
    </div>
  );
};

export default Loading;
